import React from 'react';
import { 
Box, 
} from 'grommet';

const Line = (props) => (
  <Box
    style={{width:'40%', margin:'auto', marginBottom:'8em'}}
    border={{
      'color': '#DEDEDE',
      'side': 'bottom',
    }}
  > 
  </Box>
);

export default Line